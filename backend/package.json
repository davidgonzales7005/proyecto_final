{
  "name": "proyecto-final-backend",
  "private": true,
  "devDependencies": {
    "serverless": "^3.40.0"
  },
  "scripts": {
    "deploy": "serverless deploy --region us-east-1 --stage prod",
    "remove": "serverless remove --region us-east-1 --stage prod"
  }
}
